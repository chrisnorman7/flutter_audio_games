import 'dart:math';

/// The zone for moving forward.
const forwardZone = Point(1, 0);

/// The zone for moving backwards.
const backwardsZone = Point(0, 0);

/// The zone for moving left.
const leftZone = Point(0, 1);

/// The zone for moving right.
const rightZone = Point(0, 2);

/// The zone for firing the weapon.
const fireZone = Point(1, 2);

/// The zone for going back to the main menu.
const mainMenuZone = Point(1, 1);
