name: keyboard
description: "Play piano using just the keyboard with `flutter_audio_games`."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0-210.2.beta

dependencies:
  backstreets_widgets: ^0.42.0
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_audio_games:
    path: ../../flutter_audio_games
  flutter_soloud: ^4.0.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
