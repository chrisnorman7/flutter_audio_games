name: flutter_audio_games
version: 0.19.0
description: "Widgets useful for creating audio games."
repository: https://github.com/chrisnorman7/flutter_audio_games
environment:
  sdk: ">=3.1.0<4.0.0"
  flutter: ">=1.17.0"

dependencies:
  backstreets_widgets: ^0.13.0
  dart_synthizer: ^0.11.3
  flutter:
    sdk: flutter
  flutter_synthizer: ^0.9.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
